// src/stores/hooks.js
import { useDispatch, useSelector } from 'react-redux';

// TypeScript 관련 코드 제거하고 순수 JavaScript 훅으로 변경
export const useAppDispatch = useDispatch;
export const useAppSelector = useSelector;